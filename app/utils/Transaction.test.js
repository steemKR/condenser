import assert from 'assert'

describe('Transaction Wallet', () => {
    // it('all',() => {
    //
    // })
    it('should detect private key in memo', () => {

    })
    it('should detect password in memo', () => {

    })
})

